using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Spine.Unity;

public class SpineAniCtrl : MonoBehaviour
{
    public AnimationReferenceAsset ara;
    // Start is called before the first frame update
    void Start()
    {
        //Spine.TrackEntry
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
